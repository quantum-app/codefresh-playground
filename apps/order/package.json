{
  "name": "@quantum/order",
  "version": "1.0.0",
  "main": "dist",
  "scripts": {
    "api": "yarn run:node dist/api/index.js",
    "consumer": "yarn run:node dist/consumer/index.js",
    "lint": "yarn root:lint"
  },
  "devDependencies": {
    "@types/node": "^16.11.12"
  }
}
